import { Body, Controller, Get, Param, Patch, Post, Query, Req, UnauthorizedException, UseGuards } from '@nestjs/common';import { ApiBearerAuth, ApiTags } from '@nestjs/swagger';import { Request } from 'express';import { JwtAuthGuard } from '../auth/jwt-auth.guard';import { Roles } from '../common/decorators/roles.decorator';import { Role } from '../common/enums/roles.enum';import { RolesGuard } from '../common/guards/roles.guard';import { AssignTicketDto, CreateTicketDto, ReplyTicketDto, SetTicketPriorityDto, UpdateTicketStatusDto } from './dto/support-ticket.dto';import { SupportTicketsService } from './support-tickets.service';
type AuthRequest=Request&{user?:{sub?:string}};
@ApiTags('Support Tickets')@ApiBearerAuth()@UseGuards(JwtAuthGuard)@Controller('support-tickets')export class SupportTicketsController{constructor(private readonly service:SupportTicketsService){}
@Post()create(@Req()req:AuthRequest,@Body()dto:CreateTicketDto){return this.service.create(this.user(req),dto);}@Get('mine')mine(@Req()req:AuthRequest){return this.service.myTickets(this.user(req));}@Post(':id/replies')reply(@Req()req:AuthRequest,@Param('id')id:string,@Body()dto:ReplyTicketDto){return this.service.reply(this.user(req),id,dto);}
@Get('admin/all')@UseGuards(RolesGuard)@Roles(Role.ADMIN)all(@Query('status')status?:string,@Query('priority')priority?:string,@Query('staffId')staffId?:string){return this.service.adminList({status,priority,staffId});}
@Post('admin/:id/replies')@UseGuards(RolesGuard)@Roles(Role.ADMIN)adminReply(@Req()req:AuthRequest,@Param('id')id:string,@Body()dto:ReplyTicketDto){return this.service.reply(this.user(req),id,dto,true);}
@Patch('admin/:id/assign')@UseGuards(RolesGuard)@Roles(Role.ADMIN)assign(@Param('id')id:string,@Body()dto:AssignTicketDto){return this.service.assign(id,dto);}@Patch('admin/:id/priority')@UseGuards(RolesGuard)@Roles(Role.ADMIN)priority(@Param('id')id:string,@Body()dto:SetTicketPriorityDto){return this.service.setPriority(id,dto);}@Patch('admin/:id/status')@UseGuards(RolesGuard)@Roles(Role.ADMIN)status(@Req()req:AuthRequest,@Param('id')id:string,@Body()dto:UpdateTicketStatusDto){return this.service.setStatus(this.user(req),id,dto);}
private user(req:AuthRequest){if(!req.user?.sub)throw new UnauthorizedException();return req.user.sub;}}
